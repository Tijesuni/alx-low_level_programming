#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/*
 * File: 3_function_like_macro.h
 * Desc: Header file that defines a macro named
 *       ABS(x) for finding the absolute value
 */

#define ABS(x) ((x) < 0 ? -1 * (x) : (x))

#endif
