#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc: The number of argument
 * @argv: The one-dimensional array
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	int i = 0;

	if (argc >= 1)
	{
		printf("%s\n", argv[i]);
	}

	return (0);
}
